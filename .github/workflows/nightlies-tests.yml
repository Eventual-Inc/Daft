name: Nightly testing and benchmarking suite

on:
  workflow_call:
  workflow_dispatch:

jobs:
  benchmark-local-tpch:
    uses: ./.github/workflows/benchmark-local-tpch.yml
    with:
      daft_index_url: https://d1p3klp2t5517h.cloudfront.net/builds/nightly
