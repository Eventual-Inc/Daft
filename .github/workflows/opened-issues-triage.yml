name: Move new issues into Triage

on:
  issues:
    types: [opened]

jobs:
  automate-triage-project-column:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.8.1
        with:
          project: Daft-OSS
          column: Triage
          repo-token: ${{ secrets.GITHUB_TOKEN }}
