indent-width = 4
line-length = 120
target-version = "py37"

[lint]
extend-select = [
  "UP"  # pyupgrade
]
ignore = [
  "E402"  # Module level import not at top of file [TODO(sammy): We want to fix this]
]
