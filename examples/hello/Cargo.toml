[workspace]

[package]
name = "hello"
edition = "2024"
version = "0.1.0"

[lib]
name = "hello"
crate-type = ["cdylib"]

[dependencies]
daft-ext = {path = "../../src/daft-ext"}
arrow-array = {version = "58.0.0", features = ["chrono-tz"]}
arrow-schema = "58.0.0"
