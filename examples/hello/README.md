# Hello Native Extension

A minimal Daft native extension example. See the [extension guide](../../docs/extensions/index.md) for details.

## Quick start

```bash
# Install the extension in the project .venv
uv pip install -e .

# Run the tests!
pytest -v
```
