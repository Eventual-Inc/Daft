apiVersion: v1
kind: Service
metadata:
  name: eventual-hub-proxy-external
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-backend-protocol: ssl
spec:
  ports:
    - port: 8443
      $patch: delete
    - name: https
      targetPort: 8443
      port: 443
  type: LoadBalancer
