apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../../components
patchesStrategicMerge:
- use_dev_images.yaml
secretGenerator:
- name: eventual-hub
  envs:
  - .env.dev-auth0.secret