apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: eventual-hub
bases:
- ../../components
patchesStrategicMerge:
- use_local_images.yaml
secretGenerator:
- name: eventual-hub
  envs:
  - .env.dev-auth0.secret