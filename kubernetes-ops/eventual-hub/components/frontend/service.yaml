apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-ssl-cert: <replace>
spec:
  selector:
    app: frontend
  ports:
  - name: https
    port: 443
    targetPort: 3000
  type: LoadBalancer
