apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: backend-hub-service-cert
spec:
  secretName: backend-hub-service-cert
  dnsNames:
    - "*"
  usages:
    - server auth
    - client auth
  isCA: false
  issuerRef:
    name: services-ca-issuer
