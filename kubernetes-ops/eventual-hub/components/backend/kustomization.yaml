apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ca.yaml
- cert.yaml
- deployment.yaml
- ingress.yaml
- rbac.yaml
- service.yaml
configMapGenerator:
  - name: kuberay-client-config
    files:
      - config.yaml=kuberay-client-config.yaml
