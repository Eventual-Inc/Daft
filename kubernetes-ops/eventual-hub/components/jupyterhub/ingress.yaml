apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eventual-hub-ingress-jupyter-servers
  namespace: eventual-hub
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/jupyter"
        backend:
          service:
            name: jupyterhub-svc
            port:
              number: 8000
