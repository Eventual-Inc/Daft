apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - rbac.yaml
  - secrets.yaml
  - service.yaml
  - db.yaml
configMapGenerator:
  - name: jupyterhub-config
    files:
      - jupyterhub_config.py
