apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: notebooks-ca
spec:
  secretName: notebooks-ca
  commonName: notebooks-ca
  duration: 87600h
  usages:
    - server auth
    - client auth
  isCA: true
  issuerRef:
    name: self-signed-issuer
