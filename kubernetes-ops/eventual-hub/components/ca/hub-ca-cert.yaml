apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hub-ca
spec:
  secretName: hub-ca
  commonName: hub-ca
  duration: 87600h
  usages:
    - server auth
    - client auth
  isCA: true
  issuerRef:
    name: self-signed-issuer
