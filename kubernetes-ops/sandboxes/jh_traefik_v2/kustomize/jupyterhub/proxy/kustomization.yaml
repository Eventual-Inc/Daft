apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ca.yaml
  - cert.yaml
  - deployment.yaml
  - rbac.yaml
  - service.yaml
configMapGenerator:
  - name: jupyterhub-proxy-config
    files:
      - file-provider.yml
