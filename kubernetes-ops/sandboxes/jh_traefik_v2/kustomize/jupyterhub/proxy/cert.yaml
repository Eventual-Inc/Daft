apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: proxy-cert
spec:
  secretName: proxy-cert
  dnsNames:
    - "*"
  usages:
    - server auth
    - client auth
  isCA: false
  issuerRef:
    name: proxy-client-ca-issuer
