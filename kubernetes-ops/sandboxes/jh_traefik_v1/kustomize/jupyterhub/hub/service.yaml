apiVersion: v1
kind: Service
metadata:
  name: jupyterhub
  labels:
    component: hub
spec:
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: jupyterhub
    component: hub
  type: ClusterIP
