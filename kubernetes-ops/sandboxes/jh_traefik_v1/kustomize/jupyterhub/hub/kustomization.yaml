apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - etcd-cluster.yaml
  - deployment.yaml
  - rbac.yaml
  - secrets.yaml
  - service.yaml
configMapGenerator:
  - name: jupyterhub-config
    files:
      - jupyterhub_config.py
