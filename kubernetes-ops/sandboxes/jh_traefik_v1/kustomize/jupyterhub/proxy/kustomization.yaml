apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - secrets.yaml
  - service.yaml
configMapGenerator:
  - name: jupyterhub-proxy-config
    files:
      - traefik.toml
