kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  image: kindest/node:v1.22.9
  extraMounts:
  - hostPath: ${PWD}/data
    containerPath: /data
  extraPortMappings:
  - containerPort: 30001
    hostPort: ${NOTEBOOK_HOST_PORT}
