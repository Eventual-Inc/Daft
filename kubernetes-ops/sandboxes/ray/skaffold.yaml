apiVersion: skaffold/v2beta11
kind: Config
build:
  local:
    push: false
    useBuildkit: true
  artifacts:
    - image: docker.io/library/notebook
      context: ./images/notebook
  tagPolicy:
    sha256: {}
deploy:
  kustomize:
    paths:
      - kustomize
