[dependencies]
anyhow = "1.0"
http-body-util = "0.1"
hyper = {features = ["full"], version = "1.6"}
hyper-util = {features = ["tokio"], version = "0.1"}
include_dir = "0.7.4"
parking_lot = {workspace = true}
pyo3 = {workspace = true, optional = true}
serde_json = "1.0"
log.workspace = true
serde.workspace = true
tokio.workspace = true
uuid = {version = "1.0", features = ["v4"]}
daft-recordbatch = {path = "../daft-recordbatch", default-features = false, features = ["python"]}
base64 = "0.21"
common-display = {path = "../common/display", default-features = false}
html-escape = "0.2"

[features]
default = ["python"]
python = ["dep:pyo3"]

[lints]
workspace = true

[package]
name = "daft-dashboard"
edition.workspace = true
version.workspace = true
