[build-dependencies]
tonic-prost-build = {version = "0.14"}

[package]
edition = "2021"
name = "spark-connect-generation-script"
version = "0.0.1"

[package.metadata.cargo-machete]
ignored = ["tonic-build"]

[workspace]
