syntax = "proto3";

package tron;

import "schema.proto";

message RunRequest {
  schema.Schema schema = 1;
}

message RunResponse {}

service TronService {
  rpc Run(RunRequest) returns (RunResponse) {}
}
