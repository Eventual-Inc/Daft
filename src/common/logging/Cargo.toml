[dependencies]
arc-swap = {workspace = true}
log = {workspace = true}

[lints]
workspace = true

[package]
edition = {workspace = true}
name = "common-logging"
repository = {workspace = true}
license = {workspace = true}
description = "Logging utilities for Daft"
version = {workspace = true}
