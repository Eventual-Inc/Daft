[lints]
workspace = true

[package]
edition = {workspace = true}
name = "common-version"
repository = {workspace = true}
license = {workspace = true}
description = "Version information for Daft"
version = {workspace = true}
