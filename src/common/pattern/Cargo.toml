[package]
name = "common-pattern"
repository = {workspace = true}
license = {workspace = true}
description = "Pattern matching utilities for Daft"
edition.workspace = true
version.workspace = true

[dependencies]
regex = {workspace = true}
regex-syntax = {version = "0.8"}

[lints]
workspace = true
