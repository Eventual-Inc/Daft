[dependencies]
arrow = {version = "54.1.0", features = ["ffi"]}
arrow-schema = {version = "54.1.0"}
daft-arrow = {path = "../../daft-arrow"}
pyo3 = {workspace = true, optional = true}

[features]
python = ["dep:pyo3"]

[lints]
workspace = true

[package]
edition = {workspace = true}
name = "common-arrow-ffi"
version = {workspace = true}
